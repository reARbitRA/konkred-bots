# Keep the build context small and never bake secrets or caches into a layer.

# --- Secrets -----------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key

# --- Python bytecode / caches ------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.mypy_cache/
.pytest_cache/

# Test suites are CI-only; the runtime image does not need them (and they
# pull in dev-only deps like fakeredis that are not in requirements.txt).
tests/
.ruff_cache/
.coverage
htmlcov/

# --- Virtualenvs (never reuse the host's) ------------------------------------
.venv/
venv/
env/
ENV/

# --- VCS / editors / OS ------------------------------------------------------
.git/
.gitignore
.vscode/
.idea/
.DS_Store
*.swp

# --- Build output / logs -----------------------------------------------------
build/
dist/
*.log
logs/
tmp/

# --- Docs (not needed at runtime) --------------------------------------------
README.md
